# DS Template — Kaggle Competition Workspace

Claude Code と連携するデータサイエンス・テンプレート。**このファイルは憲法**（毎ターン守る最小の規約）。
詳細は下記 4 ファイルにあり、必要な局面で読む。**判断に迷ったら `GUIDELINES.md` を開く。**

| ファイル | いつ読むか | 中身 |
|---|---|---|
| `GUIDELINES.md` | 判断・解釈・採否を決めるとき | 判断指針 `G-*` 17 件の本文（下の索引が入口） |
| `CONVENTIONS.md` | パス・命名・列・ステージを確認するとき | 決まりの辞書（判断はしない） |
| `PLAYBOOK.md` | 手順を実行するとき | 実行レシピ + 教訓アーカイブ `L-NN`（実測値つき） |
| `.claude/skills/*` | 節目で呼ぶ | 対話の進行と質問文面 |

**SSoT**: 1 情報 = 1 ファイル。重複したら下の層を削り、上の層への参照に置換する。参照の向きは常に下 → 上。
**このファイルに手順とコード例を書かない**（書きたくなったら L1/L2 へ置くサイン。表は索引に限る）。

---

## 絶対規約（違反はテンプレート違反）

- **提出前確認**: 「提出します」は意思表示であって実行許可ではない。**対象ファイル名・本日の提出回数・残り回数を提示し、明示的な同意を得てから実行する。省略禁止。**
- **1 実験 = 1 つの問い = 1 コミット**。OOF 判明後 **5 分以内**に commit（log.csv も同時。まとめコミット・バッチ更新は禁止、並行実行中も例外なし）。
- **学習 → OOF + test 予測 → 提出ファイルは 1 回の実行で出し切る**（`finalize.save_run_outputs()`）。学習だけして推論を省くと、提出時に同じ学習をやり直すことになる。
- **特徴量は 1 列ずつ計測する**（`feature_study.py`）。一括投入はスクリーニングであり、採否は必ず LOO 分解で各列の寄与を分離してから決める。
- **ステージを飛ばさない**（ゲート条件は `CONVENTIONS.md#作業ステージのゲート条件`）。
- **パッケージ管理は uv のみ**（pip・conda 禁止）。パスは必ず `src.config` から import する。
- **推定 30 分超の実験は、毎回「ローカル vs Kaggle Notebook GPU」の選択肢を提示する。**
- **「飽和確定」「全方向探索済み」は禁句。** 残り枠・終盤を理由に AI 側から探索を縮小しない。
- **ガードに引っかかったとき、ガードを緩めて通さない。** 閾値変更で自分の変更を通すのはガードを無意味にする最短経路。上限に当たったら中身を L1/L2/L3 へ出す。

---

## コマンド

**コンペ用**（`uv run python -m scripts.<名前>`）——「実験・学習・提出物を作る」もの

- `preprocess` 生データ → 特徴量 pickle（スキーマ・リーク・欠損の検証も走る）／ `train --model lgb`
  CV 学習 → OOF + test + 提出 CSV を 1 回で（`--resume` で中断再開）／ `feature_study` 1 列 ΔOOF 計測
- `optimize_hp --n-trials 25 --tag working` Optuna（study は SQLite に永続化され追加試行できる）

**ハーネス用**（`uv run python -m scripts.harness.<名前>`）——「規律を守らせる・現在地を知らせる」もの

- `doc_audit` ドキュメント階層の検査（**編集したら必ず実行**）／ `session_audit` 規律ガード一式
- `deadline_status` 現在 UTC・締切・本日の提出枠・実行時間の実測／ `job_status` 実行中ジョブの生存と ETA

---

## 判断指針の索引（本文は `GUIDELINES.md`）

**プロセス規律** — `G-PURPOSE` 目的を先に言語化し直前の学びに接続 ／ `G-CAUSAL` 因果を説明できる FE だけ提案 ／
`G-STEPWISE` 1 実験 = 1 つの問い = 1 コミット ／ `G-MECH` 規律は自己申告ではなく機械に守らせる ／
`G-BLOCKER` 技術的ブロッカーは「回避できたから解決」としない

**探索の広げ方** — `G-PERSIST` 「飽和」を宣言せず問いを立て直す ／ `G-SOURCE` モデルの多様性より情報源の多様性 ／
`G-INFOCEIL` 情報天井を判定し情報源の多様性へ切り替える

**比較と計測** — `G-FAIR` 棄却の前に比較条件が揃っているか確認 ／ `G-DIAG` OOF/LB だけで判断せず CV 内部診断を常設 ／
`G-NOISE` その差は測れるのか（量子とノイズ床を先に計算）／ `G-FULLCV` 単一分割の正の兆候はフル CV で再確認

**スコアの解釈** — `G-OOF` OOF は足切りに使い 1 位の選定には使わない ／ `G-TWOAXIS` OOF 最大化と pub_oof_gap 最小化の
二軸（gap 最大化は禁止）／ `G-OVERFIT` 「OOF↑なのに LB↓」の 3 変奏を疑う

**終盤の意思決定** — `G-CEILING` 天井帯では当てにいくのをやめ集約する ／ `G-CALIB-SUB` 提出枠は判断基準の検証にも使う

---

## セッションの作法

- **セッション開始時は `/ds-resume`。** SessionStart hook の自動提示は事実の提示であって、次の一手を合意する対話の代わりにはならない。
- **節目でスキルを呼ぶ**（kickoff / eda-visual / fe-hypothesis / new-experiment / kaggle-research / kaggle-submit / template-update）。**呼び出しが無くてもプロトコルは守る** —— 呼び出しは儀式であって規律の起動条件ではない（→ `CONVENTIONS.md#スキル呼び出しが無くても守るプロトコル`）。
- **AI から起動できないのは `/ds-kaggle-submit` だけ**（不可逆な外部作用）。黙って止まらず理由を明示し、`CONVENTIONS.md#提出の手動チェックリスト` を代替として提示する。
- **ドキュメントを編集したら `doc_audit`。** ERROR が 1 件でもあれば次に進む前に解消する。

---

## 機械ガード

hook 6 種（SessionStart / PreToolUse / PostToolUse / Stop / PreCompact / PostCompact）× ガード 5 種
（可視化・提出・診断記録・推論成果物・Public 過剰浮上）が自動で走る。**ブロックするのは可視化ガードと
提出ゲートの 2 つだけ**、他は警告。一覧と手動実行は `CONVENTIONS.md#hook-とガードの一覧`。
サブエージェント 4 つは**調査・提案・審査だけを並列化**し、学習・記録・commit はしない（`tools` で機械的に縛る。→ `CONVENTIONS.md#サブエージェントの運用規約`）。

> 守らせたい規律を見つけたら、AI への指示ではなく**ガード・hook・チェッカーとして実装できないか**を先に検討する。
> 「規約を読んだか」は観測できないが「結果が記録されたか」は観測できる（→ `G-MECH`）。
