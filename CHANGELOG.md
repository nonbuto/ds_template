# CHANGELOG — ds_template のバージョン履歴

> README から分離した版ごとの改善履歴。**README は「今の使い方」だけを載せる**
> （履歴が本文の約半分を占めると、初めて開いた人が現在の手順に辿り着けない）。
> 新しい版を足すときは、この先頭に追記する。

---

## v7.0 — 「動いている」と「正しい」を分ける

5 ラウンドの精査（うち 2 ラウンドは専門役のサブエージェント 5 体と並行）で、
**エラーを出さずに結論だけが間違う欠陥が 40 件以上**見つかった。
テンプレート自身が `G-FAIR` 違反を作り、ガードが情報を持たず、
判断の床が桁で間違っていた。v7 はその是正と、**再発を機械が検知する状態**の獲得。

### 静かに間違っていたもの（実測つき）

| 欠陥 | 実測 |
|---|---|
| 提出形式の既定が `label` | AUC で**ハードラベル提出 = −0.074** |
| early stopping が検証 fold を監視 | OOF **+0.00467** の楽観 |
| ΔOOF が改善方向に揃っていない | RMSE 系で**採否が反転** |
| `predict.py` が常に argmax | 上記の修正が**導線上のファイルだけ未適用** |
| HP 探索が本番の 1/10 の木の本数 | **−0.00097**、CB だけ 1000 本で不公正比較 |
| CB が回帰でも AUC で early stopping | 例外を出さない |
| `N_CLASSES = 3` 直書き | **clone 直後に Stage 1 が動かない** |
| importance が split なのに表示は "gain" | `G-DIAG` 第3診断軸が別の量 |
| `--resume` が `tr_score = val_score` | train−val 乖離が「乖離ゼロ」に化ける |
| ΔOOF の比較相手が自分自身／異種モデル | 常に ±0.00000、診断が `G-FAIR` 違反 |
| `blend.py` が回帰で必ず落ちる | Stage 6 が入口で全滅 |
| GroupKFold | 選べるのに**必ず ValueError** |
| 素朴な target encoding | 一意な列で **OOF AUC = 1.00000**（完全なリーク） |

### ガードとテストが「守っていなかった」

- **提出ゲートが 8 パターン中 6 件を素通り**（`uv run` 前置・`;`・改行・nohup・絶対パス）。
  さらに `bash -c` で包むと素通り、提出回数は取得失敗時に **0 を捏造**していた
- **実行時ガード 5 種を潰しても 43 件のテストが全部合格**。
  `start_run` のブロックはテストファイルに文字列すら無かった
- **予約行があると並行採番が全員同じ ID**（`['042']×8`、7 実験分の記録が消える）
- **`pub_oof_gap` ガードは情報を持っていなかった** ——
  帰無条件 93.1% / 真に危険 92.9% で発火率が同じ（→ 0.3% / 92.3% に）
- **テストが字面 grep で、変異 25 件中 13 件がすり抜け**、
  欠陥 3 件が同時に存在した状態で 98 件全件が緑だった

### 判断の床を、表から実測へ

`G-NOISE` の表は典拠に Hanley-McNeil を挙げながら **32 倍過小**の値を掲げ、
`G-DIAG` は `cv_val_std` を床にして **80 倍厳しく**、`feature_study` は
**ΔOOF 自身の SD の 1/4** の固定閾値を使っていた。**40 倍違う 2 つの床が同居**していた。

- `src/noise.py` —— 用途別に 4 経路（単体 / 行の対応差 / fold 対応差 / **提出実績**）
- **提出実績の gap SD が最も現実的な床**（分割・分布差・Public の標本ゆらぎを全部含む）。
  前コンペ最終盤の実測: `LB = OOF + 0.00112 ± 0.00007` → 床 0.00013。
  **同じ帯の隣接実験の ΔOOF は 1 件残らずその下**で、8 日・32 提出のあいだ LB 更新ゼロ。
  「飽和した」のではなく**検出可能な差を作れていなかった**
- 実証: 無関係な列の ΔOOF +0.00081 は旧閾値なら「🔶 採用検討」、新しい床では z=+0.33

### 測定装置に分割の引き直しを通した

`get_cv(seed=)` はあったのに**呼び出し 3 箇所すべてが引数なし**で、
全実験が単一の分割に条件付いていた。配線した上で測ると:

```
分割ごとの ΔOOF（無関係な列）= +0.00081, +0.00491, +0.01481, -0.00008
床の内訳: 行 1σ=0.00243 / fold 1σ=0.00126 / 分割 1σ=0.00341
```

**最も見落とされやすい成分が最大だった。**

### 足りなかった定石を実装

| 追加 | 理由 |
|---|---|
| `src/utils/encoders.py` | TE は中核 FE なのに `src/` に実装が無く毎回手書き＝毎回リークの余地 |
| `src/utils/pseudo.py` | 同上。前コンペでは**1 本も入っていないのに「寄与ゼロ」と結論**していた |
| `src/utils/postprocess.py` | 重複行の統一で AUC **+0.02025**、回帰の clip で RMSE 20.126 → 7.269 |
| `ensemble.hillclimb` / `signed_stack` | 前コンペで唯一 LB 確認できた改善の手法が**テンプレートに無かった** |
| RealMLP / TabM を第一級モデルに | NN が `feature_study` の外にいたため **FE が tree に偏って最適化**されていた |
| `src/utils/csvlock.py` | log.csv の並行更新でロックが無く、8 実験が同じ ID を取っていた |

### 再発を検知する状態にする

- テスト **43 → 160 件**。字面 grep を**振る舞い検証**へ書き換え、
  **変異注入を常設**（`tests/_mutation_check.py`、7/7 検知）
- e2e を**二値・回帰の 2 タスク**に。タスクを 1 つ増やすだけで
  blend の回帰死・実験雛形の `AxisError`・CB の `eval_metric` 残留がまとめて落ちた
- `doc_audit` C16（config 設定の文書化）を追加し、C11 が**テスト件数**も実測と突き合わせる
- 床下探索の通知（直近 8 実験がすべて床の下なら向きを変えるよう促す）
- 教訓 **L-28〜L-35** を実測値つきで追加

### 期間の配分を決めた

ステージの順序はあったが時間配分が無く、前コンペでは後半 40% が同一プールの
重み調整に費やされた。前半 50%（単体の卓越性）/ 30%（情報源の多様性）/
20%（集約と Final 2）を目安とし、**同一系統で床超えが直近 8 実験ゼロなら次へ移る**を明文化。

---

## v6.6 — 憲法と指針の分離・構成の是正

v6.5 で入れた機械ガードによって**強制力が散文から機械へ移った**ため、
常時ロードされる散文を大幅に削れるようになった。v6.6 はその再構成と、
Claude Code の想定構成との照合による是正。

### CLAUDE.md を「憲法」に絞る（26,105 → 3,342 字 / −87%）

**Claude Code 自身が CLAUDE.md に 15,000 字で警告を出す**（バイナリ内の定数、
`Large CLAUDE.md file detected`）。それまで自分で設定していた上限 27,000 字は
**公式推奨より 80% 緩い数字**で、しかも締め直すたびに「実測 + 数%」を上限にしていたため
**上限が常に現状の追認**になっていた（→ L-27）。

- **`GUIDELINES.md` を新設** —— 判断指針 `G-*` 17 件の本文と「判断の前提」（共創の原則・
  思考の外部化）を移設。**CONVENTIONS（辞書）でも PLAYBOOK（手順）でもない「判断の原則」**
  という独立した役割を持たせた
- CLAUDE.md に残したのは、4 層マップ／**絶対規約 9 件**／コマンド／**指針の索引 17 件**／
  セッションの作法／機械ガードの要約だけ
- 手順・対応表・対話の文面は L1/L2/L3 へ移設（Stage 4/5 のゲート詳細・候補プール構築・
  E[max]・強制 brainstorm・情報天井後の 4 手順・記録先の対応表・提出枠の管理方針 など）

**指針が常時ロードから降りた分、決定地点で読ませる導線を張った** ——
8 スキルすべてに「この局面で読む指針」を明記（`ds-kickoff` → `G-SOURCE`/`G-PURPOSE`、
`ds-kaggle-research` → `G-PERSIST`/`G-INFOCEIL` など）。

### doc_audit: C12 を追加（12 チェック）

分離した構造で最も壊れやすいのは「索引にあるが本文が無い／本文はあるが索引に無い」。
**C12（指針の索引と本文の一致）**を新設し、両方向の乖離を機械検知する。
あわせて検査対象に `GUIDELINES.md` を追加、C1 の上限を 5,000 字へ、
C3 の ID 定義元を GUIDELINES.md へ移した。

### Claude Code 準拠の是正

バイナリから認識パスを特定して照合した。**構成自体は適合**（`.claude/settings.json` の hooks、
`.claude/skills/<name>/SKILL.md`、`agents`/`commands`/`rules`/`plugins` は任意で未使用）。
`/doctor` の `parse_error` 等はキーバインド検査で無関係と判明。是正は 2 点:

- **`.claude/settings.local.json` を `.gitignore` へ。** プロジェクト側に無く、
  ユーザーのグローバル gitignore に偶然救われていた —— clone 先では個人の許可設定が
  追跡対象になる。残骸（死んだ PID・コンペ固有パス・セッション UUID）も掃除
- **`scripts/` を 2 系統に分離。** 判定基準は「実験・学習・提出物を作る」（`scripts/` 10 本）か
  「規律を守らせる・現在地を知らせる」（**`scripts/harness/`** 10 本）か。
  `ROOT` を `parents[2]` に統一し、hook のコマンド・相互 import・文書の参照を更新した
  （履歴ファイルの CHANGELOG / TODO_ARCHIVE は各時点の事実なので書き換えない）

### 教訓

**L-27**: 上限は「自分で決めた数字」ではなく「外部が定める数字」で測る。
現状を基準に線を引く限り、どれだけ締めても「今の大きさは適正」という結論しか出ない。

---

## v6.5 — 規律の機械化（ハーネス化）

`G-MECH`（規律は AI の自己申告ではなく機械に守らせる）は v6 の旗印だったが、
**テンプレート自身にはほとんど適用されていなかった**。強制の入口は PostToolUse hook 1 本のみで、
s6e8 で実際に破られた規律（提出前確認・1 実験 1 コミット・状態ファイルの更新）は
いずれも機械化ゼロの側にあった。v6.5 はここを埋める。

### 強制機構: hook 1 種 → **6 種** / ガード 2 種 → **5 種**

| hook | 実行 | 役割 |
|---|---|---|
| `SessionStart` · `PostCompact` | `session_brief.py` | 現在地の提示。**圧縮直後の再注入**（長時間セッション運用で効く） |
| `PreToolUse` | `submit_gate.py` | Kaggle 提出に**ユーザー承認を要求**（`permissionDecision: "ask"`）。枠・締切・git は実測値 |
| `PostToolUse` | `viz_guard.py` | 可視化・診断記録・推論成果物・Public 過剰浮上 |
| `Stop` | `session_audit.py` | コミット規律・状態ファイルの鮮度（非ブロック） |
| `PreCompact` | `session_snapshot.py` | 圧縮直前に SESSION.md へ退避 |

**ブロックしてよいのは 2 つだけ** —— 実績のある可視化ガードと、不可逆な提出ゲート。

### 学習成果を失わない仕組み

- `src/utils/finalize.py` — **学習 → OOF + test 予測 → 提出 CSV を 1 回の実行で出し切る**。
  学習だけして推論を省くと、提出時に同じ学習をやり直すことになる（実測で多発）
- `src/utils/multiseed.py` — multi-seed avg で**基本 seed を再利用**（avg5 の学習時間が 1/5 削減）
- `src/utils/foldcache.py` — **fold 単位チェックポイント**。中断した学習を途中から再開できる
  （4 時間超まわした学習を fold 4/25 で打ち切り全損した事故への対策）
- `experiments/.running/` + `job_status.py` — 実行中ジョブの生存・進捗・ETA・ハング検知

### 観測できるようにしたもの

`duration_sec`（30 分ルールの推定を較正する実測。列追加は既存 log.csv を壊さず移行）/
`state_audit.py`（状態ファイルの停滞。FEATURE_REPORT が 3 週間止まった事故の検知）/
`hook_status.py`（**hook が本当に発火したか**の実測）/
`feature_report --sync`（「今どの特徴量がベースか」を実測から機械生成）

### 死蔵の解消

`src/feature_registry.py` を削除（import 元ゼロ）。`src/validation.py`（252 行・リーク検出を含む）を
`preprocess.py` に配線し、**書かれているのに一度も走らないコード**を実際のガードにした。
`train.py` が規約（提出まで 1 フロー・`feature_names` の記録）を破っていた状態も解消。

### ドキュメント階層

- **C1 を行数から文字数へ**。行数で測っていたため、箇条書きを 1 行に結合するだけで
  測定値が下がり、**上限を守りながら中身が 6% 増えていた**（行数 −2 / 文字数 +1,898）
- CLAUDE.md **32,112 → 26,105 字**。手順・対応表・文面を L1/L2/L3 へ移設
- 完了済み TODO を `docs/TODO_ARCHIVE.md`、版履歴を `CHANGELOG.md` へ分離
- 教訓 **L-24〜L-26** を追加

### 既知の未達

Claude Code 組み込みの CLAUDE.md 警告閾値は **15,000 字**で、現状 26,105 字はこれを 74% 超過する。
次版で「憲法 60 行 + `GUIDELINES.md`」への再構成を予定。

---

## v6 系で追加された主な改善 — ドキュメント階層の再設計

v5 で s6e7 の総括を反映した結果、**毎セッション自動ロードされる CLAUDE.md が 1,070 行**まで膨らんだ。
`.claude/rules/` の 153 行も自動注入されるため、常時ロードは合計 1,223 行。
調査したところ、これは肥大化ではなく**設計ルール自体の欠陥**だった（改善プロトコルが「教訓 → CLAUDE.md」と
定めていたため、教訓ブロック 31 個・105 行が原則ファイルに沈殿していた）。

### ① 4 層モデルと SSoT（Single Source of Truth）

| 層 | ファイル | ロード | 役割 |
|---|---|---|---|
| **L0 原則** | `CLAUDE.md` | 毎セッション自動 | 毎ターン守る「判断の憲法」 |
| **L1 規約** | `CONVENTIONS.md` *(新設)* | オンデマンド | 引く「辞書」。判断しない、決まりだけ |
| **L2 手順+史料** | `PLAYBOOK.md` | オンデマンド | 実行レシピ + **教訓アーカイブ（SSoT）** |
| **L3 進行台本** | `.claude/skills/*` | 起動時のみ | 対話フェーズと質問文面 |

SSoT 4 原則: ①同じ文が 2 箇所に現れたら下の層を削り上の層への参照に置換する ②参照の向きは常に下→上
③**教訓の本文は `PLAYBOOK.md#教訓アーカイブ` にのみ置き、L0 には「結論 + 鍵の数値 + リンク」の 1 行だけ許す**
④L0 にコードフェンスを書かない。

> **③で「数値は残す」のが要点**。数値は教訓の payload であり、これが消えると規範が
> 「守るべきもの」でなくなる。教訓 16 件は `L-01`〜`L-17` として PLAYBOOK へ移設した。

### ② 指針 #1〜#31 → 恒久 ID 17 項目

通し番号は統合のたびに全ファイルの参照が壊れる（v5 では「ヘッダーが `#1-22` のまま」「`#20` と `#17` の
取り違え」等のバグが 8 件蓄積していた）。**恒久スラッグ ID** に移行し、統合しても ID が壊れないようにした。

`G-PURPOSE` `G-CAUSAL` `G-STEPWISE` `G-PERSIST` `G-SOURCE` `G-FAIR` `G-DIAG` `G-NOISE` `G-OOF`
`G-TWOAXIS` `G-OVERFIT` `G-FULLCV` `G-INFOCEIL` `G-CEILING` `G-CALIB-SUB` `G-MECH` `G-BLOCKER`

主な統合 — `G-PERSIST` ← #4/#7/#8/#12/#14（「飽和」禁句と Discussion 調査が三重）、
`G-OVERFIT` ← #20/#24/#25（「OOF↑なのに LB↓」の同一メカニズム 3 変奏）、
`G-MECH` ← #9/#16（#16 は #9 に完全包含）。旧番号との対応表は `CONVENTIONS.md` 付録A（v6 限りの移行用）。

### ③ `scripts/doc_audit.py` — SSoT を機械検証する

「AI に守らせたい規律は、AI に守らせようとしてはいけない」（`G-MECH`）をドキュメント自体にも適用した。

| チェック | 内容 |
|---|---|
| C1 | 常時ロードの行数予算（> 650 行で ERROR） |
| C2 | `CLAUDE\|PLAYBOOK\|CONVENTIONS.md#…` のアンカー解決 |
| C3 | 恒久 ID の解決 / 旧番号 `指針#N` の残存 |
| **C4** | **実測値の保存**（固定 37 個の数値が消えたら ERROR）|
| C5 | 重複ブロック検知（正規化後 3 行以上の一致） |
| C6 | SSoT 違反（キーフレーズ → 所有ファイルの辞書） |
| C7 | コンペ識別子の混入（L0/L1/L3 は 0 でなければならない） |
| C8 | SESSION.md 上限値の同期 |
| C9 | L0 内のコードフェンス数（0 が目標） |
| **C10** | **孤立節検知** — L1/L2 に「どこからも参照されない節」が無いか |
| **C11** | **README の同期** — README の自己申告値（行数・C4 個数・チェック数・スキル一覧）が実測と一致するか |

> **C4 が主役**。行数や見出しの diff では「教訓の実測数値が静かに消えた」ことを検知できないが、
> 数値の grep なら 100% 検知できる。今回の再編で **一度も落とさずに完了した**（現在 37/37）。

`/ds-template-update` のフェーズ2.5 に `doc_audit` の実行を組み込み、**テンプレート改善の直後に必ず検査が走る**。

### ③-2 「読んだか」ではなく「結果が記録されたか」を測る（v6.2）

v6 で `.claude/rules/` を CONVENTIONS.md へ移した際、**skills から読ませる導線を張り忘れる**実装漏れが起きた
（自動ロードされていた内容が「誰も読まない場所」になった）。同じ失敗を機械的に防ぐため 2 つ追加した:

- **C10 孤立節検知** — オンデマンド層の節が誰からも参照されていなければ WARNING。
  実際にこの実装漏れを再現したところ、失った 2 節を正確に指摘した
- **診断記録ガード**（`_check_diagnostic_recording_guard()`）— 直近 10 実験の `cv_train_mean` / `cv_val_std`
  記入率が 70% を切ったら警告。tracker を経由しない使い捨てスクリプトが `G-DIAG` を空洞化させる問題を、
  **結果の側から**検知する（過去コンペの実 log.csv で検証: 0/10 = 0% を正しく検出）

さらに **C11** は README 自身を対象にする。README は「このテンプレートが何であるか」の対外的な宣言であり、
実態からずれると**次にこの repo を開いた人（未来の自分）が誤った前提で作業を始める**。
行数・C4 の個数・チェック数・スキル一覧の件数を実測と突き合わせ、ズレたら WARNING を出す
（v6.3 で README の更新漏れを人が先に見つけたため機械化した。当時の状態を再現すると 2 件を正しく検出する）。

> **設計原則**: 「規約を読んだか」は観測できないが「**結果が記録されたか**」は観測できる。

### ③-3 リリース後に見つけて直したもの（v6.1 / v6.3）

再編そのものが持ち込んだ欠陥と、本番投入前の点検で見つけた不整合:

| 版 | 内容 |
|---|---|
| **v6.1** | `.claude/rules/` を CONVENTIONS.md へ移した際、**skills から読ませる導線を張り忘れていた**（自動ロードされていた内容が「誰も読まない場所」になった）。5スキルに明示 Read を追加。この失敗を機械化したのが上記 C10 |
| v6.1 | TODO_TEMPLATE の残 1 件「不安定NN の HP 調整はサブサンプル単一fold で信頼できない」を `G-FULLCV` へ反映（実測は L-18）。未完了 TODO は **0 件** |
| **v6.3** | 雛形の `/resume` → `/ds-resume`（Claude Code 標準と衝突する。`ds-` 接頭辞はまさにこの衝突を避けるためのもの）。`av_check.py` に FEATURES 空チェック追加（無関係な LightGBM エラーで落ちていた） |

### ④ 実測

| | v5 | v6 |
|---|---|---|
| **常時ロード**（CLAUDE.md + `.claude/rules/`） | **1,223 行** | **650 行（-47%）** |
| CLAUDE.md 内のコンペ識別子 | 18 | **0** |
| CLAUDE.md 内のコードフェンス | 15 | **0** |
| 旧番号 `指針#N` の参照 | 36 | **0** |
| 既知バグ | 8 | **0** |

**規範文の消失検証**: `git diff` の削除行から「禁止 / 必ず / 義務 / 省略 / してはならない」を含む
規範候補 56 行を全数抽出し、新体系での所在を 1 行ずつ照合した（実質消失は 1 行のみで、復元済み）。

### ⑤ その他

- `.claude/rules/`（153 行）を削除し `CONVENTIONS.md` へ移設 — 内容が 100% 規約リファレンスなのに
  常時ロードの 12.5% を占めていた。L1 と役割が完全に重なるため残すと SSoT が再び破れる
- `FE_HYPOTHESES.md` に**索引セクション**を新設 — 過去コンペで 2,395 行まで肥大化し、
  `/ds-resume` の全文 Read が削減分を 1 回で帳消しにしていた。50 件超からは索引のみ Read する
- `/ds-eda-report` を削除（廃止済みだったが description が毎セッションのシステムプロンプトを占有していた）

---

## v5 で追加された主な改善

s6e7 の Private LB 確定後に全 169 提出を事後検証し、**「判断の仕方」そのものの欠陥**を修正した版。
最大の変更は、**規律を AI の自己申告に委ねるのをやめ、実行環境側の機械的検知に移した**こと。

### ① 可視化ガード — 「AI に守らせたい規律は、AI に守らせようとしない」

同じ失敗を **2 コンペ連続** で繰り返した（可視化が最初の数日に集中し以降ほぼゼロ）。
第1世代「努力目標」→ 第2世代「発動条件の明示」がいずれも実効性を持たなかったため、第3世代として機械的強制に移行:

- `src/experiment.py: _check_visualization_guard()` — log.csv の直近5実験のタイムスタンプと `data/output/plots/*.png` の更新時刻を比較して自動判定。`end_run()` の末尾で必ず実行される
- `scripts/viz_guard.py` — CLI ラッパー（tracker を経由しない使い捨て実験もカバー）
- `.claude/settings.json` — PostToolUse hook。log.csv が更新された直後にのみ判定を走らせる
- CLAUDE.md `G-MECH` に**失敗の履歴そのもの**を記載（3度目を防ぐため）

### ② 天井帯での意思決定 — 「当てにいく」のをやめ「集約する」

候補間の差がノイズ床に近い領域では、OOF も Public も**上位の順序を予測できない**（実測: OOF 上位10件 ∩ Private 上位10件 = **1件**）。

- **`G-CEILING`**: 天井帯では単一最良を選ばず**集約**する（重み探索の bagging / 上位 N 構成の平均）。実測で Private 最高を記録したのは「12 シードの重み探索を平均した」構成だった
- **`G-INFOCEIL`**: **情報天井の判定**手順（argmax 一致率 + セグメント別分解）と、判定後のアーキテクチャ探索の打ち切り
- **`G-OOF` に但し書き**: 「OOF で足切りする」は有効／「OOF で 1 位を選ぶ」は無効。機械的に OOF 最高を選ぶと最悪手になりうる（実測: OOF 最高候補の Private は天井帯でも下位）
- PLAYBOOK に手順6（集約への切り替え）・手順7（情報天井の判定）を追加

### ③ CV 内部診断を常設の判断軸に（`G-DIAG`）

OOF と LB は「結果の点数」しか教えない。**同じ OOF でも中身の健全性は違う**。

- 3診断軸（train−val gap / fold間 std / importance）を OOF と必ず併記する
- **ΔOOF が fold 間 std より小さいなら、その差は「測れていない」** — `end_run()` が自動判定して表示する
- 診断マトリクス（train↔val 乖離 × val↔LB 乖離）で原因を切り分ける。**「大×大」では正則化に飛びつく前に校正不足を疑う**
- `/ds-kaggle-submit` フェーズ3で、**ユーザーが指摘する前に AI 側から**乖離3値を提示する運用に変更

### ④ 棄却プロセスの強化 — 不公正比較による誤棄却を防ぐ

s6e7 では、**最終的に全モデルの土台となった中核 FE を AI が当初「棄却」していた**（旧 HP を新特徴量セットに流用した不公正比較が原因）。ユーザーの押し戻しで採用に転じた。

- **`G-FAIR` にチェック4「不公正比較チェック」**を追加（HP / 特徴量セット / CV 条件 / 較正の4項目）
- **特徴量数が ±20% 以上変動した場合の棄却は、HP 再調整前は「棄却」として記録してはならない**（暫定保留とする）
- **`G-CALIB-SUB`**: 提出枠を「良い候補を出す」だけでなく**「判断基準そのものを検証する」**ためにも使う（1日1枠を検証提出に）

### ⑤ 探索の方向づけ — 情報源の多様性とアーキテクチャの多様性は違う

- **`G-PERSIST` 追記**: 情報天井が実証されたときこそ、AI は「探索を縮小する提案」ではなく**「問いを立て直す提案」**を出す（Kickoff 前提の再検証 / 棄却済み仮説の再評価 / 外部調査のやり直し / 評価指標の性質の測り直し）
- **`G-PERSIST` 強化**: 「未試行の情報次元」5個のうち**最低2個は現データに存在しない変数**でなければならない（既存変数の組み合わせで枠を埋めることを禁止）
- **`G-SOURCE` 強化**: 1つの外部データの否定的結果を全体に一般化しない／**行レベル照合で「同じ生成プロセスのデータか」を最初に確認する**
- **`G-NOISE` の適用範囲拡張**: 量子（1行あたりの指標変化）の計算を、目標設定だけでなく**LB 観察の解釈**にも必須化

### ⑥ 実装・インフラの修正

- **multiclass 対応**: `scripts/train.py` / `predict.py` / `optimize_hp.py` が二値分類専用だったのを解消（`N_CLASSES` で切り替え、`run_cv()` の汎用 CV 骨格を追加）
- **一括投入ガード**: `scripts/feature_study.py` で複数列の同時投入に `--allow-batch --batch-reason` を必須化。理由は log.csv に記録される（**一括はスクリーニング、判断は LOO 分解後**）
- **log.csv の行分裂を解消**: `/ds-new-experiment` の予約行と `end_run()` が別行を作る問題を、予約行の検出・上書きマージで修正
- **依存関係の移植性回復**: arm64-only の `[tool.uv] environments` 制限を撤廃し、`numba>=0.60` の直接依存だけで解決するよう変更
- **`G-BLOCKER`**: 技術的ブロッカーを「回避できたから解決」としない運用 + PLAYBOOK に「既知の落とし穴（ライブラリ別）」の章を新設

---

## v4.2 で追加された主な改善

s6e6 振り返りで未反映だった教訓を消化し、「特定コンペへの最適化」ではなく「汎用DSプロセス」として純化した。

### ① 全スキル名を `ds-` プリフィクスに統一

Claude Code 標準スキルとの衝突防止（`resume` が標準 `/resume` と衝突した前例）。
`/ds-` + Tab でテンプレートスキルのみを一覧できる。
「テンプレートのスキルは必ず `ds-` で始まる」を規約化（例外なし）。

### ② 評価指標の汎用化

ΔAUC 表現を ΔOOF に統一し、採否閾値は `G-NOISE`（Public LB ノイズ床）/ #20（OOF 改善閾値）の
指標別テーブルへ接続。AUC 以外（RMSE / Logloss / Accuracy）のコンペでもそのまま機能する。

### ③ コンペ戦略軸の事前言語化（`/ds-kickoff` Q7）

「スコア最大化 / 自前モデルの限界追求 / 新手法の習得」等の重視軸を序盤に COMPETITION.md へ記録し、
Final 2 選定時に必ず再掲。スコア期待値と戦略軸が対立する場合は AI が両論併記し、**ユーザーが決定する**。

### ④ プロセス実効性の強制力化

- **可視化の必須発動条件**: LBベスト更新時・oof_lb_gap 大変動時・5実験連続可視化ゼロ時（努力目標→実施義務）
- **「1実験スクリプト = 1つの問い」**: 複数仮説の混在禁止。AI の効率化目的の設計変更は実行前確認
- **逐次記録チェック**: `/ds-resume` が未コミット実験を検出・警告。`is_best` 列を廃止し SESSION.md 一元管理
- **知識移転の確認**: FE のメカニズムを平易な言葉で説明し理解確認を取ってから実装（記録 ≠ 知識移転）

### ⑤ 運用の正確性

- **提出枠は UTC 基準**: 自己申告カウント廃止、`kaggle competitions submissions` の実結果から逆算
- **30分ルール**: 推定30分超の実験は毎回「ローカル vs Kaggle Notebook GPU」を提示（`/ds-kickoff` でHW確認）

### ⑥ FE 探索効率

- **表現バリエーションの事前枝刈り**: 同一観察への複数表現（percentile/z-score/interaction）は、1つ目棄却時に既存特徴量との情報源重複を特定してから次へ
- **外部シグナルFE × アーキテクチャ相性**: Tree系で棄却でも NN系で評価してから確定（帰納バイアス差）

---

## v4.1 で追加された主な改善

s6e6 の全実験（104本）を振り返り、「スコア上昇」と「user との共創」の観点でテンプレートを精査。
原則を1つも削らずに、探索効率と認知負荷を改善した。

### ① 外部知見調査をワークフロー前段に配置

`/ds-kaggle-research` を「行き詰まりの応急処置」から「主軸アーキテクチャ決定の前提入力」へ格上げ。

- **フェーズ0（序盤アーキテクチャ調査）** を新設。Stage 1.5 の直前に上位カーネルのアーキテクチャ分布を調べ、サーベイ候補に反映する
- Playground Series は上位解法のアーキテクチャがスコアを最も動かす。それを終盤ではなく序盤の方向づけに使う

### ② CLAUDE.md の2層分離（Core + PLAYBOOK）

CLAUDE.md 1246行 → 734行（-41%）。実行レシピを `PLAYBOOK.md` へ分離（v6 で 4 層へ再設計）。

- **CLAUDE.md** = 毎ターン守る「原則・判断基準（精神）」
- **PLAYBOOK.md** = その局面で読む「実行レシピ（手順・コード・コマンド）」
- 判断に必要な情報だけで意思決定でき、実行時に該当セクションを参照する。認知負荷を下げてルール形骸化を防止

### ③ FE 仮説にドメイン実体を引き出す Q0 を追加

`/ds-fe-hypothesis` の因果メカニズム掘り下げに **Q0（ドメイン実体の確認）** を新設。

- AI が因果を推測する前に、user のドメイン知識から変数の実体（物理的/業務的な意味）を引き出す
- 「相関がありそう」から「この物理量がこう振る舞うからターゲットが決まる」という鋭い仮説へ
- 「共創の原則」を Kickoff だけでなく FE サイクル全体へ展開

### ④ 提出のたびに「伸びしろの所在」を可視化

`/ds-kaggle-submit` に LBトップとの差分＋その差を埋める最有力候補（アーキテクチャ/情報次元/多様性）を毎回提示。

- 局所改善（+ノイズ床の FE）に没入して最大の伸びしろ（別アーキテクチャ等）を見落とすことを防ぐ
- 埋めるべきは「構造的な伸びしろ」であって Public LB スコアそのものではない（`G-NOISE` / `G-OOF` / `G-TWOAXIS` と整合）

---

## v4 で追加された主な改善

s6e6 実践（3クラス Balanced Accuracy）を経て、アーキテクチャ探索戦略と OOF/LB 評価軸を大幅強化。

### AI 行動規範 #21-22（CLAUDE.md）

- **#21**: OOF最大化 + pub_oof_gap最小化の二軸評価（gap最大化戦略を廃止）
  - 実証: OOF→Private r=+0.998、pub_oof_gap→シェイクダウン量 r=+0.853
  - 第一目標: OOF最大化。第二目標: 同OOF水準なら pub_oof_gap 最小化
  - モデルファミリー別 OOF 信頼性テーブル（NN系/Tree系/Blend の傾向）
- **#22**: アーキテクチャ乗り換え時の公正比較義務
  - 同一特徴量セット × 作業用HP確定済み × 同一CV の3条件を揃えてから比較
  - 「最適化済み LGB vs デフォルト HP の RealMLP」は不公正比較として禁止

### Stage 1.5（早期アーキテクチャサーベイ）新設

FE探索を始める前に「主軸アーキテクチャ」を決定するステージを追加。

```
Stage 1（最小ベースライン）完了後 → Stage 1.5 → Stage 2（EDA）開始
```

- 候補アーキテクチャ（LGB / CatBoost / RealMLP 等）を最小特徴量 × 作業用HP × 同一CVで評価
- 各アーキテクチャの OOF と pub_oof_gap を記録し、主軸を1つ決定
- 副軸（主軸と OOF 差 10% 以内）は Stage 6 アンサンブル候補として記録

> **教訓**: 過去コンペで LGB 主軸のまま 40+ 実験を費やした後、RealMLP に移行したら提出効率が 50x 改善（+0.000007 LB/提出 → +0.000343 LB/提出）。Stage 1.5 で早期に特定すべきだった。

### FE 有効性のアーキテクチャ依存性明記

- LGB で棄却した FE が RealMLP では有効なケースに対応
- FE 棄却記録に「棄却したアーキテクチャ名」の明記を義務化
- Stage 4 → Stage 6 移行時に全候補アーキテクチャへ同一 FE を移植して再評価

### Kaggle Notebook 環境サポート追加

`src/config.py` が `/kaggle/input` の存在を自動検出し、パスを切り替える。

```python
from src.config import IS_KAGGLE, RAW_DATA_DIR, OOF_DIR
# ローカル: IS_KAGGLE=False、RAW_DATA_DIR=data/raw/
# Kaggle:  IS_KAGGLE=True、RAW_DATA_DIR=/kaggle/input/
```

---
