---
name: resume
description: セッション開始時に現在地を復元する。SESSION.md・log.csv・FE_HYPOTHESES.md を読み込み、「今どこにいるか・次に何をするか」を1画面でブリーフィングする。
---

# /resume スキル

## このスキルの役割

新しいセッションを始めるとき、または文脈が途切れたと感じたときに呼ぶ。
複数のファイルを手動で読む代わりに、このスキルが「現在地」を1画面に集約する。

**いつ使うか:**
- 新しいチャットセッションを開始したとき
- 前回の会話内容が不明確になったとき
- 「今どこにいたっけ？」と感じたとき

---

## 実行手順

### フェーズ1: 状態ファイルの読み込み（実行層）

以下のファイルをすべて読み込む:

1. `SESSION.md` — 現在地スナップショット（最優先）
2. `experiments/log.csv` — 直近5件の実験記録
3. `FE_HYPOTHESES.md` — 仮説のステータス集計
4. `COMPETITION.md` — コンペ基本情報（Stage 0 の記録）
5. `TODO_TEMPLATE.md` — HIGH優先度の未完了項目

### フェーズ2: ブリーフィングの出力（実行層）

以下の形式で現在地を出力する:

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 /resume — セッション復元ブリーフィング
 （SESSION.md + log.csv より自動生成）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【現在のステージ】
  Stage X — <ステージ名>
  状況: <SESSION.md の「フェーズ内の状況」>

【スコア状況】
  ベースラインOOF : <値>  (exp<ID>)
  現在ベストOOF   : <値>  (exp<ID>)
  現在ベストLB    : <値>  (exp<ID>)
  OOF/LBギャップ  : <差分> （<大きい/小さい/ほぼ一致>）

【直近の実験（最新3件）】
  exp<NNN> | <モデル> | OOF=<値> | LB=<値> | <1行サマリー>
  exp<NNN> | <モデル> | OOF=<値> | LB=<値> | <1行サマリー>
  exp<NNN> | <モデル> | OOF=<値> | LB=<値> | <1行サマリー>

【FE仮説の状態】
  ✅ 採用: N件  ❌ 棄却: N件  🔵 未検証: N件
  次の未検証候補: H-<NNN> <タイトル>

【前回セッションで決めた次のアクション】
  1. <SESSION.md の「次にやること」より>
  2. ...

【未解決の問い・ブロッカー】
  - <SESSION.md より>

【今サイクルの重要方針】
  - <SESSION.md の「重要な方針」より>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
続けますか？ 上記の「次のアクション」から始めてよいですか？
（内容に変更があれば教えてください）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### フェーズ3: 確認と接続（思考層）

ブリーフィングを出力した後:

1. **「前回の続きから始めてよいですか？」** と確認する
2. ユーザーが変更や補足を言ったら、SESSION.md の「次にやること」または「未解決の問い」を更新する
3. ユーザーが「はい」または「続けて」と言ったら、「次のアクション」の1番目を実行し始める

---

## 注意事項

- SESSION.md が存在しない（プロジェクト開始直後）場合は「SESSION.md がまだ作成されていません。`/kickoff` から始めてください」と案内する
- log.csv が存在しない場合は「実験記録がありません。最初の実験を始めましょう」と案内する
- FE_HYPOTHESES.md が存在しない場合は仮説セクションを省略する
- 状態ファイルの内容が古い可能性があることをユーザーに伝え、「追加の変更があれば教えてください」と促す
